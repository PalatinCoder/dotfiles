set $wskpxc 
bindsym $mod+p workspace $wskpxc
workspace $wskpxc output eDP-1

# match title, not appid, so the browser dialog popups stay on their workspace
assign [title="^(.*)KeePassXC$"] $wskpxc
