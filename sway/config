# Read `man 5 sway` for a complete reference.

# as much wayland as possible ;)
xwayland disable

# include all the things
include ~/.config/sway/config.d/*
include /etc/sway/config.d/*

# lock the screen when starting sway (usefull when using autlogin on the tty)
exec swaylock --image $wallpaper --grace 0

# autotiling[AUR] (utility to automatically split on the longest edge)
exec autotiling
